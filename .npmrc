# 发布用:本仓库的包发到官方 registry。
# 本机用户级 ~/.npmrc 指向的是只读镜像(registry.npmmirror.com),而
# package.json 的 publishConfig.registry 已经钉了官方 registry;
# 这个文件是显式再声明一次,免得哪天 publishConfig 被改掉后静默发到镜像上失败。
# 它包含凭据时不会被提交:authToken 只写在用户级 .npmrc / keychain 里。
registry=https://registry.npmjs.org/
