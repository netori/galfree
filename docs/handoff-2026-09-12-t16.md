# Handoff — GALFree(dsh-galfree)· 参考链一致性回路已交付(T16),下一会话:T17 起

> 写给一个**全新 agent**(可能跑在另一个模型上)。本文件只补"会话内才有的知识";
> 一切已入册的内容以路径/编号引用,不重复。日期:2026-09-12(前两场交接见
> `docs/handoff-2026-09-12.md` 剧本环节、`docs/handoff-2026-09-12-t14-t15.md` 素材前半,
> 那两份仍然有效 —— 尤其后者 §3 的 Ren'Py 实测事实与 §7 的工具坑,别再重查)。

## 1. 一分钟现状

- 仓库 `E:\DSH_project\DSH_creator`,分支 `main`,本会话 **4 个 commit**(`d98458c..47e4f4f`),
  交付 **T16(#24)**,工作区干净。
- 工单 GitHub `netori/galfree`:**只剩 #25(T17)、#26(T18)+ #1(Spec)**。
- 测试:**快带 21 文件 177 测试**全绿;**慢带 4 文件 32 测试通过 + 1 跳过**(真 SDK 那 3 条 +
  新的真上游链验证,后者**默认跳过**)。
- 本会话**没有**碰用户项目(`D:\GALGAME\hjm` 一个字节没动)。

## 2. 本会话最大的设计决定(照抄,别重造)

**链上的图,远端拿不到 —— 所以要内联。**

登记簿的参考图是**项目内的文件**(`game/images/x.png`),而 `image_url` 要的是可取的地址。
所以发送前经 `WriteGateway.readBytes` 读字节、转 **data URL**(`dataUrlOf`),
`image_url` 收它。三条边界:

- 账本里**仍然只有路径**(引用);图片内容只在这一次出网里出现,**不落 `.studio/`**;
- 建任务时筛"文件在不在"(不在 → `degradation.code='reference-missing'` 并列出丢了哪张);
- 跑的时候若文件中途被删 → **如实报错**,不少发一张了事("链看着生效了其实没有"是要防的假象)。

**这条推理链还没有在真上游上验证过**(见 §5:慢带写好了但没跑)。如果真上游不认 data URL,
要改的是 `referenceField()` 那一处(协议形状),不是整条回路。

**差分批量的顺序是派生出来的**:`sortSlotsByReference` 按"谁的产物出现在别人的参考链里"
排序(主视觉先出),**不是**"槽名里带 base 的先出"。并且**建一个跑一个** ——
差分建任务时主视觉已经落盘,链才是真的。

## 3. 本会话踩过/查明的事实(不在任何仓库文件里)

- **`gh issue view` 会打印 issue 之外的东西**;读工单用 `gh issue view <n> --repo netori/galfree`。
- **`~/.dsh/settings.yaml` 里有明文密钥**(`dsh-galfree.imageApiKey` 与 `dsh-imagegen.channelSecrets`)。
  读设置文件做排查时**先脱敏**;我这一次的 `-replace` 只挡了 `apiKey:`,漏了 `channelSecrets:`,
  密钥进了会话记录 —— **别再犯**:整段替换或只读需要的键。
- **慢带里"跳过"必须出声**:`live-chain.slow.test.ts` 在缺环境变量时 `console.warn` 打印怎么跑,
  并 `describe.skipIf`。一条"悄悄不跑"的守卫等于没有守卫(这条慢带是故意这么设计的)。
- **守卫要验它能红**:本会话对 `differential-grid.test.ts` 做了变异验证(把 `role` 恒为
  `'variant'`,那条用例立刻红,再还原);`live-chain.slow.test.ts` 用不可达端点验过会红。
- **`.scratch/commit-*.txt` 是本仓库写 commit message 的既有做法**(PowerShell here-string 写中文会坏文件;
  用 `write` 工具写文件再 `git commit -F`)。

## 4. 交付内容(T16 · #24 三条 AC 的落点)

| AC | 落在哪 |
|---|---|
| 差分批量任务参数含正确链引用(**假上游断言**) | `src/service/reference-chain.test.ts`(10 条):假上游收到的请求体里 `image[0].image_url` 解出来**正是主视觉那张图的字节** |
| 对比视图渲染自登记簿 + 槽位历史 | `ProjectService.differentialGrid()` + `src/service/differentials.ts`(`differential-grid.test.ts` 5 条;含"调它不产生任何写"的断言)、工作台素材板第三个 tab |
| 拒收注记进任务历史并可被 agent/人回读 | `GenerationTask.rejections[]`(只追加)、`retryGenerationTask({note,via})`、`galfree_art_queue` 回 `rejections`、面板格子里的"拒收理由"输入框 |

另外:真模型链验证入慢带(`live-chain.slow.test.ts`)、`GET /asset` 只读素材字节(面板缩略图)、
角色视图新增**参考链编辑**(主视觉登记进链的入口)。

## 5. 未了事项(下一会话优先)

1. **真上游链验证还没跑**(要用户点头,它花钱:真出 2 张图)。跑法:
   `GALFREE_LIVE_BASE_URL` / `GALFREE_LIVE_API_KEY` / `GALFREE_LIVE_MODEL`(可选
   `GALFREE_LIVE_ADAPTER=async-task`)→ `npm run test:slow -- src/service/live-chain.slow.test.ts`。
   用户渠道是 `https://api.seedance.nz/v1` + `zhenzhen-image-g-v2.5-flare`,`adapter: async-task`。
   **结论按慢带打印的原话回报,不要美化**:上游不接受就如实说"该模型的 referenceChain 声明未被证实"。
2. **人工验收(需要重启宿主才生效)**:素材板第三个 tab「差分对比」、角色视图的「参考链」编辑、
   格子里的"重 roll + 拒收理由"。**插件改了 `lib/` 必须重启宿主**(`dsh plugin add link:` 不热挂载);
   重启后确认 `Get-Process -Name "DSH Desktop"` 的启动时间晚于 `lib/` 构建时间。
3. **上一会话遗留仍未做**:在 `hjm` 上点一次试玩、看窗口能否正常退出(无头验不了"玩通");
   `alice` 还没进角色登记簿(warning)。
4. **T17(#25)是下一张票**(BGM/SE 接线),之后 T18(#26,本地发布 `build_dists`)。
5. 发版前重跑慢带;插件永不 push 用户项目仓。

## 6. 环境与流程

- 快带 `npm test`;慢带 `npm run test:slow`(**串行**,并行会抢 SDK 与游戏窗口造成假失败)。
  跳过大下载:`$env:GALFREE_SDK_DIR = "$env:USERPROFILE\.dsh\dsh-galfree\sdk"`。
- 宿主侧与客户端侧是**两个 bundle**(`lib/index.js` / `lib/client.js`),`npm run build` 一起出。
- 校验面板能不能真渲染:`.scratch/harness/live-gui.mjs`(playwright + 宿主 Chromium profile,
  打 127.0.0.1:43120)。**它看到的是"当前跑着的宿主"**,所以插件改动必须先重启宿主。

## 7. 硬规矩(违反前先提 ADR 修订)

写一律过网关;`.rpy` 唯一真相、`.studio/` 永不复制叙述内容(参考链只存路径与备注,
不存图片内容也不存台词);进度纯推导、审读戳只人盖;**不依赖 dsh-imagegen**;
SDK 钉版;v1 只从模板新建;插件永不 push 项目仓、不改写用户手动 commit。
**动用户项目文件前必须明确点头。**

## 8. Suggested skills(下一 agent 请用 skill tool 调用)

- `tdd-zh` / 领 T17 开工的入口(每张票红→绿切片;**新写的守卫必须验一次它能红**)。
- `diagnosing-bugs-zh` — 先建能红的回路再读代码。
- `code-review-zh` — 每张票完成后双轴审查(固定点取上一次修复 commit)。
- `wizard-zh` — 需要人参与的多步操作(重启后验收、真上游首跑)。
- `frontend-design` / `ui-ux-pro-max` — 差分对比面继续进化时。

## 9. 用户与沟通

中文交流;愿意给自主权,但要求**如实呈现**(不谎称通过、不静默降级),**明确点头才动范围**。
他**看不懂设计稿/截图里的领域细节** —— 描述要给"他要看什么、会看到什么",别丢术语。
他机器上已装真 SDK 与配好的渠道:**真跑上游会花他的额度,跑之前说一声**(本会话**没有**跑)。
